import resume from "../cvj_resume.png";

const Resume = () => {
  return (
    <div style={align}>
      <img src={resume} width="800px" alt="" />
    </div>
  );
};

const align = {
  textAlign: "center",
  marginTop: "50px",
};

export default Resume;
